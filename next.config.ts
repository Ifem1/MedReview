import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack config left empty — disables Turbopack-specific options
  // Run with: next dev (webpack is used when turbopack key is absent)
};

export default nextConfig;
